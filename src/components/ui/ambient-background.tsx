/** Subtle CSS-only ambient orbs — no JS, no extra deps */
export function AmbientBackground() {
  return (
    <div className="ambient-orbs" aria-hidden>
      <span />
      <span />
      <span />
    </div>
  );
}
